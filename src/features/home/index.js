// Public API of the `home` feature.
// Import features through this barrel (e.g. `import { HomePage } from '@/features/home'`)
// so a feature's internal file layout can change without breaking callers.
export { HomePage } from './page/homePage'
