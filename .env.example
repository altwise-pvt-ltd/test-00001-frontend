# Base URL for the backend API. Falls back to `/api` (dev proxy) when unset.
# Must use https and include the /api prefix the backend mounts routes under.
VITE_API_BASE_URL=https://test-00001-backend.vercel.app/api
